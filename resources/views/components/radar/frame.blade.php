<div class="frame hidden">
    <img src="{{ $frameSet['image'] }}">
    <span class="frame-time">{{ $frameSet['time'] }}</span>
    <div class="logo-container">
        <img src="{{ $icon }}" class="brand-icon"><span class="brand">{{ $brand }}</span>
    </div>
</div>
